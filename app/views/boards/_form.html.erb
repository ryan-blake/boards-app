<%= form_for(board) do |f| %>
  <% if board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board.errors.count, "error") %> prohibited this board from being saved:</h2>

      <ul>
      <% board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions  text-center">
    <%= f.hidden_field :user_id, :value => @user.id %>

  <div class="well">
      <div class="well">
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control' %>
       </div>

      <div class="well pull-right">
      <br>
        <div class="form-group">
          <%= f.label :make %>
          <%= f.text_field :make, class: 'form-control' %>
        </div>

        <div class="form-group">
          <p> age of board<p>
          <%= f.label :age %><br/>
         <%= f.number_field :age %>
       </div>
<!--
        <div class="form-group">
          <%= f.label :lake %>
          <%= f.collection_select :lake_id, Lake.all, :id, :name, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :type %>
          <%= f.collection_select :type_id, Type.all, :id, :name, class: 'form-control' %>
        </div>

     -->

        <!-- <div class="form-group">
          <%= f.label :propulsion %>
          <%= f.collection_select :propulsion_id, Propulsion.all, :id, :name, class: 'form-control' %>
        </div> -->

        <div class="form-group">
          <p> price per day <p>
          <%= f.label :price %><br/>
         <%= f.number_field :price %>
        </div>
        <div class="form-group">
          <%= f.label :length %><strong> Bow to Stern </strong><br/>
         <%= f.number_field :length %>
        </div>


      </div>

    <div class="form-group" style="display:flex;">
      <%= f.label :description %>
      <%= f.text_area :description, class: 'form-control' %>
    </div>
  </div>

<!-- <div class="well">
  <strong> Address released to Users after payment is received. </strong>
    <div class="form-group">
       <%= f.label :address %><br/>
       <%= f.text_field :address %><br/>
     </div>

     <div class="form-group">
      <%= f.label :city %><br />
      <%= f.text_field :city %>
     </div>
     <div class="form-group">
      <%= f.label :state %><br />
      <%= f.text_field :state %>
     </div>
     <div class="form-group">
      <%= f.label :zipcode %><br />
      <%= f.number_field :zipcode %>
     </div>
   </div> -->
<!--
    <div class="form-group">
      <p> image <p>
      <%= f.label :spot_image %><br/>
     <%= f.file_field :spot_image %>
    </div> -->

    </br>

    <div class="btn btn-primary  text-center">
      <%= f.hidden_field :user_id, :value => @user.id %>
      <!-- <%= f.hidden_field :is_boat, :value => true %> -->
    <%= f.button%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
