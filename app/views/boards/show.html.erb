<p id="notice"><%= notice %></p>
<div class="btn">
  <% if current_user && current_user.id == @board.user_id%>
    <%= link_to "Edit", edit_board_path %>
  <% end %>
</div>

<div class="container-fluid">
<div class="jumbotron">
  <div class="well well-sm pull-left">
  <tr>
    <td><h2><%= @board.title %></h2></td>
    <td><h3><%= @board.type.name %>board</h3></td>
    <strong>
      <%= @board.make %>Brand
      </strong>
      <br>
     <!-- bindings/trucks/stomp pad -->
     <% if @board.type_id == 1 %>
      <strong>Stomp pad </strong>
      <% elsif @board.type_id === 2 %>
      <strong> Bindings </strong>
      <% else %>
      <strong> Trucks </strong>
      <% end %>
      <% if @board.footgear == true %>
        Yes  <% else %>  No:
      <%end %>
    </tr>
    <strong> board age: </strong> <%= @board.age %> Years
 </div>
 <div class="">
  <div style="text-align:center;"class="well well-sm ">

     <h3> Dimensions</h3>
     <strong>Length:</strong><%= @board.length %><br>
     <strong>Width:</strong><%= @board.width %><br>
     <% if @board.type_id == 1 %>
     <strong>volume:</strong><%= @board.volume %><br>
     <% end %>
   </div>
 </div>
<div class="well well-sm pull-right">
 <%= @board.description %>
</div>
<tr>
  <hr style="padding-top:30px;">
<% @board.images.each do |i| %>
  <td style="" class="center-text" ><%= attachment_image_tag(i, :file, :limit, 400, 400) %></td>
<% end %>
</tr>
</div>

<hr>

<div class=" text-center">
 <div class="well">
   <% if @board.pending != true && @board.user_id != current_user.id%>
  <p> $<%= @board.price %> </p>
    <%= render partial: 'charge', locals: {board: @board} %>
  <% elsif  @board.user_id != current_user.id  %>
    <%= render partial: 'charge', locals: {board: @board} %>
  <% elsif @board.arrived === true %>
  To list for sale check yes
      <%= render partial: 'forsale', locals: {board: @board} %>
  <% elsif @board.arrived == false  && @board.user_id == current_user.id%>
  <%= render partial: 'unlist', locals: {board: @board} %>
 <% end %>

 <br>

 </div>
</div>
</div>


<%= link_to 'Back', boards_path %>
