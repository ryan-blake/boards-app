$('#results').html('<%= escape_javascript render 'search_signed_in' %>');
$('#partial-s').append('<%= j render @boards %>');
<% if @posts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @boards %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
