
<%= simple_form_for @event, remote: true do |f| %>
  <div class="form-inputs">
    <%= f.input :tag %>
    <%= f.input :date_range, input_html: { style: "z-index:999;", class: "form-control input-sm date-range-picker" } %>
    <%= f.input_field :start_time, as: :hidden, value: Date.today, class: 'form-control input-sm start_hidden' %>
    <%= f.input_field :end_time, as: :hidden, value: Date.today, class: 'form-control input-sm end_hidden' %>
    <%= f.input :color, as: :select, collection: [['Black','black'], ['Green','green'], ['Red','red']] %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
    <%= link_to 'Delete',
                event,
                method: :delete,
                class: 'btn btn-danger',
                data: { confirm: 'Are you sure?' },
                remote: true unless @event.new_record? %>
  </div>
<% end %>
